import React from "react";
import { Button as BMC} from '@mantine/core';

const Button = (props) =>{
    return (
        <BMC {...props}>
            Teste
        </BMC>
    )
}

export default Button;